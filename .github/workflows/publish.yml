name: Release

# SKELETON — not wired to actually publish yet. Needs an NPM_TOKEN repo
# secret (an npm automation token for the @manthan org/scope) before the
# `changesets/action` publish step below can succeed; until then this runs
# safely and fails cleanly at that one step. See CLAUDE.md roadmap item 2.

on:
  push:
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    # `secrets` isn't readable in a job/step `if:` at all (GitHub Actions
    # restriction — only `env` is) — mirror the secret into env so the last
    # step can skip cleanly instead of failing when NPM_TOKEN isn't set yet.
    env:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - name: Checkout manthan-icons
        uses: actions/checkout@v4
        with:
          repository: tech-manthan/manthan-icons
          path: manthan-icons

      - name: Checkout manthan-base
        uses: actions/checkout@v4
        with:
          repository: tech-manthan/manthan-base
          path: manthan-base

      - name: Checkout manthan-vue
        uses: actions/checkout@v4
        with:
          path: manthan-vue

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Build manthan-icons
        working-directory: manthan-icons
        run: |
          npm ci
          npm run build

      - name: Build manthan-base
        working-directory: manthan-base
        run: |
          npm ci
          npm run build

      - name: Install manthan-vue
        working-directory: manthan-vue
        run: npm ci

      - name: Build manthan-vue
        working-directory: manthan-vue
        run: npm run build

      - name: Create a "Version Packages" PR, or publish when one is merged
        if: ${{ env.NPM_TOKEN != '' }}
        uses: changesets/action@v1
        with:
          cwd: manthan-vue
          version: npm run version-packages
          publish: npm run release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
